package com.teamteorganiza.estoque.model;

/**
 * Unidade em que um produto é controlado no estoque.
 * UN = unidade, KG = quilo, L = litro, CX = caixa, PCT = pacote.
 */
public enum UnidadeMedida {
    UN, KG, L, CX, PCT
}
